import styled from 'styled-components';
import { BCYlw } from '../../colors';

export const EditCon = styled.div`
    position: relative;
`
export const ESpan = styled.span`
    cursor: pointer;
    color: ${BCYlw};
    font-weight: 600;
    font-size: 14px;
    position: absolute;
    top: 10px;
    right: 0;
`